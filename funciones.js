// Función con nombre
function calcularPerimetroCirculo(radio) {
    let perimetro = 2 * 3.1415 * radio;
    return perimetro;
  }
  
  // Función flecha
  let calcularPerimetroCirculoFlecha = (radio) => 2 * Math.PI * radio;

  //calcular el area de un rectangulo 
  function CalcularAreadeunrectangulo (base, altura) {
    let area = base * altura;
    return area;
  }
  //calcular area de un rectangulo funcion flecha 
  let CalcularAreadeunrectanguloflecha = (base, altura) => base * altura 

// Calcular el cuadrado de un numero
  function Calcularelcuadradodeunnumero (numero) {
     let cuadradodeunnumero= numero * numero;
     return cuadradodeunnumero;
    }
 
//calcular el cuadrado de un numero flecha 

    let Calcularcuadradodeunnumeroflecha = (numero) => numero * numero

//calcular conversion de grados celsius a fahrenheit 

  function Calculargradoscelsiusafarenheit (celsius) {
   let gradosfarenheit = (celsius * 9 / 5) + 32;
   return gradosfarenheit;
  }

  let calculargradoscelsiusafarenheitflecha = (celsius) => (celsius * 9 / 5) + 32


  

  




